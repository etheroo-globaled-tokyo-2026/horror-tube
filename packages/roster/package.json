{
  "name": "@horror-tube/roster",
  "private": true,
  "scripts": {
    "venv": "python3 -m venv .venv && .venv/bin/pip install -q -r requirements.txt",
    "test": ".venv/bin/python -m unittest discover tests"
  }
}
